# mdcli 渲染规则文档

## 色彩体系

| 元素 | ANSI 码 | 效果 |
|------|---------|------|
| H1 | `1;37;45` | 粗体白字 + 紫红底 |
| H2 | `1;34` | 粗体蓝字 |
| H3 | `1;36` | 粗体青字 |
| H4 | `1;32` | 粗体绿字 |
| H5 | `1;33` | 粗体黄字 |
| H6 | `2;37` | 暗灰字 |
| 粗体 | `1;31` | 粗体红字 |
| 斜体 | `3` | 斜体 |
| 粗斜体 | `1;3;31` | 粗体斜体红字 |
| 删除线 | `9` | 删除线 |
| 行内代码 | `1;32` | 粗体绿字 |
| 链接 | `4;34` | 下划线蓝字 |
| 图片占位 | `2;35` | 暗紫字 |
| 引用竖线 | `36` | 青色 |
| 引用文字 | `2` | 暗色 |
| 表头 | `1;4` | 粗体+下划线 |
| 表格边框 | `2` | 暗色 |
| 分割线 | `2` | 暗色 |
| 无序列表符 | `36` | 青色 |
| 有序列表数 | `33` | 黄色 |
| Todo-完成 | `1;32` | 绿色实心圆 ● |
| Todo-待办 | `2` | 灰色空心圆 ○ |

---

## 块级元素渲染规则

### 标题（H1-H6）
- H1 前缀 `■`（黑方块），H2-H6 前缀 `▸`（三角箭头）
- 无下划线装饰
- 层级通过颜色 + 前缀区分

### 段落
- 正常颜色，自动换行到终端宽度
- 行内格式（粗体/斜体/代码等）正常渲染

### 代码块（围栏）
```
┌ python ──────────────────
│ def hello():
│     print("world")
└──────────────────────────
```
- 框线包围（`┌─│─└` 字符），边框为暗色
- 顶边框中显示语言标签
- 代码内容原样输出（无背景色、无语法高亮）
- 宽度自适应：取最长行 + 4 或终端宽度 - 2，最小 20
- 超长行截断 + `…`

### 引用块
```
▎ 这是引用文字
▎ 多行引用
```
- `▎` 青色竖线 + 暗色文字
- 空行显示裸竖线

### 无序列表
```
  • 列表项
  • 带**粗体**的项
```
- 青色 `•` 符号，缩进 2 空格
- 续行缩进 4 空格

### 有序列表
```
  1. 第一步
  2. 第二步
```
- 黄色数字，缩进 2 空格

### 任务列表（Todo）
```
  ● 已完成的任务
  ○ 待完成的任务
```
- `- [x]` → 绿色 `●` 实心圆
- `- [ ]` → 灰色 `○` 空心圆
- 可与普通列表混用

### 表格

```
┌──────────────┬──────────────┬──────────────┐
│ 列1          │ 列2          │ 列3          │
├──────────────┼──────────────┼──────────────┤
│ 数据         │ 内容         │ 值           │
└──────────────┴──────────────┴──────────────┘
```
- Unicode 框线字符（`┌┬┐├┼┤└┴┘│─`）
- 表头：粗体 + 下划线
- 边框：暗色
- 列宽自动分配：最小 4，超出终端宽度时按比例收缩
- **单元格内支持行内格式**（`` `code` `` → 绿色，`**bold**` → 粗体）
- **长文本自动换行**，换行边界 ANSI 码自动关闭/重开
- 对齐：`:---` 左对齐，`:---:` 居中，`---:` 右对齐

### 分割线
```
────────────────────────────────
```
- 满终端宽度 `─`，暗色
- 仅当行不含 `|` 时才识别为分割线（避免与表格混淆）

### 图片
- 渲染为 `[IMG: alt文本]`，暗紫色

### 链接
- 文字：蓝色下划线
- URL：在文字后以暗色括号显示，当 URL ≠ 文字时：`文字 (url)`

---

## 行内格式优先级（高→低）

1. `` `code` `` — 行内代码
2. `![alt](url)` — 图片
3. `[text](url)` — 链接
4. `***text***` — 粗斜体
5. `**text**` — 粗体
6. `*text*` — 斜体（需左右都不是 `*`）
7. `__text__` — 粗体（备选语法）
8. `_text_` — 斜体（备选语法，需左右都不是 `_`）
9. `~~text~~` — 删除线
